{
  "name": "book-api",
  "version": "1.0.0",
  "main": "src/app.js",
 "scripts": {
  "start": "node src/app.js",
  "test": "mocha --timeout 10000 --exit --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results/results.xml"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.3"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "chai-http": "^4.3.0",
    "mocha": "^10.2.0",
    "mocha-junit-reporter": "^2.2.1",
    "mongodb-memory-server": "^10.1.4",
    "supertest": "^7.1.1"
  }
}
