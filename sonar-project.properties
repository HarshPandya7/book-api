# Required: Must match EXACTLY what's in your local SonarQube UI (case-sensitive)
sonar.projectKey=book-api-project
sonar.projectName=Book API

# Optional but recommended
sonar.projectVersion=1.0.0

# Source directory (use '.' for root or specify 'src' if applicable)
sonar.sources=.

# Exclusions (critical for Node.js to avoid false issues)
sonar.exclusions=node_modules/**,test/**,coverage/**,*.log,.git/**,.github/**,.idea/**,Jenkinsfile,docker-compose.yml

# Language-specific settings (Node.js/JavaScript)
sonar.language=js
sonar.javascript.lcov.reportPaths=coverage/lcov.info # If using Jest/Mocha with coverage

# sonar.host.url is now injected by Jenkins with 'withSonarQubeEnv', so it's not strictly necessary here,
# but can remain if you want to run sonar-scanner locally without Jenkins.
<<<<<<< HEAD
# sonar.host.url=http://localhost:9000
=======
# sonar.host.url=http://localhost:9000
>>>>>>> 1b1d66f (Update sonar-project and other dependencies)
